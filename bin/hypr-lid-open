#!/bin/sh
. "$HOME/.bin/hypr-utils"

enable_internal

if has_external_monitors; then
    sleep 1
    mon=$(get_internal_mon)
    [ -n "$mon" ] && hyprctl dispatch moveworkspacetomonitor 1 "$mon"
    [ -n "$mon" ] && hyprctl dispatch moveworkspacetomonitor 2 "$mon"
fi
